#' @param as_sf `logical(1L)`, default: `maritime_as_sf()`
#' * Whether an `sf` ([sf::st_sf()]) should be returned. 
#' * Ignored if the `{sf}` package is not installed.
#' * `maritime_as_sf()` defaults to `TRUE`.
