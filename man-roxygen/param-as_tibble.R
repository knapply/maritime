#' @param as_tibble `logical(1L)`, default: `maritime_as_tibble()`
#' * Whether a [tibble::tibble()] should be returned.
#'  * Ignored if the `{tibble}` package is not installed.
#'  * `maritime_as_tibble()` defaults to `TRUE`.
